APP_NAME=China Date Toolkit API
APP_VERSION=1.0.0
DEBUG=false
HOST=0.0.0.0
PORT=8080

# RapidAPI 鉴权（部署时在 Render/Vercel 环境变量配置，本地开发留空即跳过）
RAPIDAPI_PROXY_SECRET=

# 管理接口密钥（保护 /refresh 等管理端点，部署时在环境变量配置）
ADMIN_TOKEN=

# 请求限流（每 IP 每分钟请求次数）
RATE_LIMIT=60/minute
